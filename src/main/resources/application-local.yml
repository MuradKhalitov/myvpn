spring:
  datasource:
    url: jdbc:postgresql://localhost:5454/myvpn
    username: myvpn
    password: myvpn

payment:
  allow-fake: true

vpn:
  delivery:
    enabled: ${VPN_DELIVERY_ENABLED:false}
  provider:
    allow-fake: true
